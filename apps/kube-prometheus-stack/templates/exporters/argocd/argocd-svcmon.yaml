---
# Application controller (fixes your original — add namespaceSelector)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: app-of-apps:monitoring.coreos.com/ServiceMonitor:monitoring/argocd-metrics
  name: argocd-metrics
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - argocd
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-metrics
  endpoints:
    - port: metrics
      interval: 30s
---
# API server
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: >-
      app-of-apps:monitoring.coreos.com/ServiceMonitor:monitoring/argocd-server-metrics
  name: argocd-server-metrics
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - argocd
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-server-metrics
  endpoints:
    - port: metrics
      interval: 30s
---
# Repo server (Service has two ports; must pin to the "metrics" one)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: >-
      app-of-apps:monitoring.coreos.com/ServiceMonitor:monitoring/argocd-repo-server-metrics
  name: argocd-repo-server-metrics
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - argocd
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-repo-server
  endpoints:
    - port: metrics
      interval: 30s
---
# ApplicationSet controller (also has a "webhook" port — pin to "metrics")
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: >-
      app-of-apps:monitoring.coreos.com/ServiceMonitor:monitoring/argocd-applicationset-controller-metrics
  name: argocd-applicationset-controller-metrics
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - argocd
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-applicationset-controller
  endpoints:
    - port: metrics
      interval: 30s
---
# Notifications controller
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: >-
      app-of-apps:monitoring.coreos.com/ServiceMonitor:monitoring/argocd-notifications-controller-metrics
  name: argocd-notifications-controller-metrics
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - argocd
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-notifications-controller-metrics
  endpoints:
    - port: metrics
      interval: 30s
---
# Dex server (optional — only if you use SSO via Dex; skip if unused)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  annotations:
    argocd.argoproj.io/tracking-id: >-
      app-of-apps:monitoring.coreos.com/ServiceMonitor:monitoring/argocd-dex-server-metrics
  name: argocd-dex-server-metrics
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - argocd
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-dex-server
  endpoints:
    - port: metrics
      interval: 30s